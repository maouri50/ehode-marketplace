import { CartDrawer } from "@/components/CartDrawer";
import { ProductCard } from "@/components/ProductCard";
import { StoreHeader } from "@/components/StoreHeader";
import { trpc } from "@/lib/trpc";
import { ArrowRight, ChevronRight, Download, Search, ShieldCheck, Sparkles } from "lucide-react";
import { useMemo, useState } from "react";
import { Link } from "wouter";

export default function Home() {
  const { data: products = [], isLoading, error } = trpc.storefront.catalog.list.useQuery({});
  const { data: categories = [] } = trpc.storefront.catalog.categories.useQuery();
  const [query, setQuery] = useState("");
  const [category, setCategory] = useState<string | null>(null);
  const visible = useMemo(() => products.filter((product) => (!category || product.categoryHandle === category) && `${product.title} ${product.description ?? ""} ${product.category ?? ""}`.toLowerCase().includes(query.toLowerCase())), [products, category, query]);
  return <div className="store-page marketplace-page"><StoreHeader/><main><section className="marketplace-hero"><div className="container marketplace-hero__grid"><div className="marketplace-hero__copy"><span className="marketplace-kicker"><Sparkles size={14}/> Digital goods for curious makers</span><h1>Find your next<br/><em>good idea.</em></h1><p>Templates, design bundles, and printable resources chosen for the projects you actually want to finish.</p><div className="marketplace-hero__actions"><a href="#collection" className="marketplace-primary">Shop all downloads <ArrowRight size={17}/></a><a href="#how-it-works" className="marketplace-text-link">How digital delivery works <ChevronRight size={16}/></a></div><div className="marketplace-hero__benefits"><span><Download size={15}/> Instant access after payment</span><span><ShieldCheck size={15}/> Secure PayPal checkout</span></div></div><div className="marketplace-hero__image marketplace-hero__image--illustrated" aria-label="Abstract creative desk illustration"><span className="hero-art__paper hero-art__paper--one"/><span className="hero-art__paper hero-art__paper--two"/><span className="hero-art__pen"/><div className="marketplace-hero__badge"><strong>Made to make</strong><span>your next project easier</span></div></div></div></section><section className="marketplace-categories" id="categories"><div className="container"><div className="marketplace-categories__heading"><span>Browse by category</span><a href="#collection">View all <ChevronRight size={15}/></a></div><div className="marketplace-categories__row"><button className={!category ? "is-selected" : ""} type="button" onClick={() => { setCategory(null); setQuery(""); document.getElementById("collection")?.scrollIntoView({ behavior: "smooth" }); }}>All downloads</button>{categories.map((item) => <button type="button" className={category === item.handle ? "is-selected" : ""} key={item.id} onClick={() => { setCategory(item.handle); setQuery(""); document.getElementById("collection")?.scrollIntoView({ behavior: "smooth" }); }}>{item.name}<small>{item.description}</small></button>)}</div></div></section><section className="marketplace-catalog container" id="collection"><div className="marketplace-catalog__intro"><div><span className="eyebrow">Shop the collection</span><h2>Explore digital downloads.</h2></div><p>Thoughtful tools for print, design, and everyday creative work.</p></div><div className="marketplace-search-row"><label className="marketplace-search" id="catalog-search"><Search size={18}/><span className="sr-only">Search the collection</span><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search templates, SVGs, printables and more"/></label><div className="marketplace-search-row__result"><strong>{visible.length}</strong> {visible.length === 1 ? "result" : "results"}</div></div><div className="marketplace-filter-row"><button className={!category ? "is-active" : ""} type="button" onClick={() => setCategory(null)}>All</button>{categories.map((item) => <button type="button" className={category === item.handle ? "is-active" : ""} onClick={() => setCategory(item.handle)} key={item.id}>{item.name}</button>)}</div>{isLoading ? <div className="product-grid product-grid--skeleton"><div/><div/><div/><div/></div> : null}{error ? <div className="catalog-state"><h3>The collection is taking a moment.</h3><p>Please refresh to try again.</p></div> : null}{!isLoading && !error && visible.length === 0 ? <div className="catalog-state"><h3>No matching downloads found.</h3><p>Try a different word or return to the whole collection.</p><button type="button" onClick={() => { setQuery(""); setCategory(null); }}>Show all downloads</button></div> : null}{visible.length > 0 ? <div className="product-grid marketplace-product-grid">{visible.map((product) => <ProductCard key={product.id} product={product}/>)}</div> : null}</section><section className="marketplace-reassurance" id="how-it-works"><div className="container marketplace-reassurance__grid"><div><span className="eyebrow">A simple digital shop</span><h2>Choose it today.<br/>Use it your way.</h2></div><div className="marketplace-reassurance__items"><article><span>01</span><div><strong>Browse with ease</strong><p>Search by project type, collection, or creative need.</p></div></article><article><span>02</span><div><strong>Pay safely with PayPal</strong><p>Your payment is handled securely through PayPal checkout.</p></div></article><article><span>03</span><div><strong>Download after purchase</strong><p>Your purchase-linked file access appears once payment is confirmed.</p></div></article></div></div></section><section className="marketplace-studio container" id="about"><div><span className="eyebrow">Meet Ehode</span><h2>Small digital goods, carefully chosen.</h2><p>Ehode starts as a focused one-seller studio, building a clear and useful collection of creative resources. More marketplace features can come later; every product is curated from one place today.</p></div><div className="marketplace-studio__card"><Download size={24}/><strong>Digital by design</strong><span>Protected delivery after verified payment, with no waiting for physical shipping.</span><Link href="/admin">Admin workspace <ChevronRight size={15}/></Link></div></section></main><footer className="store-footer marketplace-footer"><div className="container"><span className="store-brand">ehode<span>.</span></span><p>Digital downloads for makers, planners, and creative projects.</p><small>© {new Date().getFullYear()} Ehode. Independent digital goods.</small></div></footer><CartDrawer/></div>;
}
