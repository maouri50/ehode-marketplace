import DashboardLayout from "@/components/DashboardLayout";
import { trpc } from "@/lib/trpc";
import { ArrowLeft, Download, ReceiptText } from "lucide-react";
import { Link } from "wouter";

export default function OwnerOrders() {
  const { data: orders = [], isLoading, error } = trpc.storefront.owner.orders.useQuery();
  return <DashboardLayout><div className="mx-auto max-w-6xl space-y-7 pb-10"><Link href="/owner" className="inline-flex items-center gap-2 text-sm font-bold text-[#b94b5d]"><ArrowLeft size={15}/> Back to owner workspace</Link><section className="rounded-[1.5rem] border border-border bg-[linear-gradient(120deg,#fffaf4,#f3e4dc)] p-7 md:p-9"><div className="flex items-start justify-between gap-5"><div><p className="text-xs font-bold uppercase tracking-[0.18em] text-[#ad4b58]">PayPal activity</p><h1 className="mt-3 font-serif text-4xl tracking-[-0.06em] text-[#2d251f]">Orders and download access.</h1><p className="mt-4 max-w-xl text-sm leading-6 text-muted-foreground">Every confirmed PayPal capture is stored here with its delivery access grants. Buyer receipt links remain private and are never displayed in this workspace.</p></div><ReceiptText className="text-[#b94b5d]" size={28}/></div></section>{error ? <section className="rounded-xl border border-destructive/30 bg-destructive/5 p-5 text-sm text-destructive">This page is available to the project owner only.</section> : null}<section className="overflow-hidden rounded-2xl border border-border bg-card"><div className="flex items-center justify-between border-b border-border px-6 py-5"><div><p className="text-xs font-bold uppercase tracking-[0.16em] text-[#ad4b5a]">Order records</p><h2 className="mt-1 font-serif text-2xl tracking-[-0.04em]">Latest payments</h2></div><Download className="text-muted-foreground" size={20}/></div>{isLoading ? <div className="p-6 text-sm text-muted-foreground">Loading PayPal order records…</div> : null}{!isLoading && !orders.length ? <div className="p-8 text-sm leading-6 text-muted-foreground">No orders have been recorded yet. After you attach a product file and complete a verified PayPal payment, each order and its download grants will appear here.</div> : null}{orders.length ? <div className="overflow-x-auto"><table className="w-full min-w-[720px] text-left text-sm"><thead className="bg-muted/55 text-xs uppercase tracking-[0.12em] text-muted-foreground"><tr><th className="px-6 py-3 font-semibold">Buyer</th><th className="px-6 py-3 font-semibold">Status</th><th className="px-6 py-3 font-semibold">Total</th><th className="px-6 py-3 font-semibold">Items</th><th className="px-6 py-3 font-semibold">Downloads</th><th className="px-6 py-3 font-semibold">Date</th></tr></thead><tbody>{orders.map(({ order, itemCount, grantCount }) => <tr className="border-t border-border" key={order.id}><td className="px-6 py-4">{order.buyerEmail ?? "Guest checkout"}</td><td className="px-6 py-4 capitalize">{order.status}</td><td className="px-6 py-4">{new Intl.NumberFormat("en", { style: "currency", currency: order.currencyCode }).format(Number(order.totalAmount))}</td><td className="px-6 py-4">{itemCount}</td><td className="px-6 py-4">{grantCount}</td><td className="px-6 py-4">{order.createdAt ? new Date(order.createdAt).toLocaleDateString() : "—"}</td></tr>)}</tbody></table></div> : null}</section></div></DashboardLayout>;
}
